spring:
  main:
    keep-alive: true
  datasource:
    url: jdbc:postgresql://localhost:5432/daily_dev_db
    username: iqwrwq
    password: ${DB_PASSWORD:812421}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

dailyDev:
  task:
    cron: "0 0 10,16,20 * * *"  # täglich um 10, 16 und 20 Uhr
  anthropic:
    apiKey: ${ANTHROPIC_API_KEY}
  git:
    repoUrl: https://github.com/dev-arts-de/dev-arts-java-wissensdatenbank.git
    localPath: /opt/dailydev/repo
    token: ${GITHUB_TOKEN}
    branch: main
  ntfy:
    topic: jscope
    baseUrl: https://ntfy.sh
  vercel:
    baseUrl: ${VERCEL_BASE_URL}
